let firstName = "Draven";
let jobTitle = "Software Engineer";
let city = "New York";

let newJobPost = `${firstName] is a ${jobTitle} based in ${city}.`

document.getElementById("result").innerHTML = newJobPost;
